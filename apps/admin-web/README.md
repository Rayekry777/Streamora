# Streamora Admin Web

独立运营管理端 Vue 应用，不加载用户端会话或宠物运行时。阶段 1 提供桌面布局、路由和 RBAC 导航骨架。

```bash
pnpm --filter @streamora/admin-web dev
pnpm --filter @streamora/admin-web test
pnpm --filter @streamora/admin-web build
```

