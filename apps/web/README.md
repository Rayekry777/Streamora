# Streamora Web

用户端与创作者端 Vue 应用。阶段 1 提供路由、状态、请求缓存和全局宠物宿主骨架；视频、社区和 Agent 业务在后续里程碑接入。

```bash
pnpm --filter @streamora/web dev
pnpm --filter @streamora/web test
pnpm --filter @streamora/web build
```

