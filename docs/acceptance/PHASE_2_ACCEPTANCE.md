# 阶段 2 验收

- 状态：待验收
- 更新日期：2026-08-15
- 目标：实现用户登录、管理登录、细粒度 RBAC，以及公共宠物到个人宠物的安全切换。

## 交付物

- 已实现：用户与管理员隔离的账户、凭据、会话、Cookie 和 CSRF 契约。
- 已实现：管理端五个内置角色、权限映射和受保护路由。
- 已实现：公共宠物读取、登录后个人宠物读取与全局宿主原位切换。
- 已实现：两个前端的独立登录流程、会话状态和权限拒绝状态。

## 验证证据

| 检查项 | 实际执行 | 结果 |
|---|---|---|
| 阶段转换 | 用户明确要求继续推进 | 阶段 1 已完成，阶段 2 开始 |
| HTTP 契约 | OpenAPI lint 与 TypeScript 类型生成 | 通过；保留 1 个阶段 1 游标分页未使用警告 |
| RPC 契约 | Protobuf 编译与 Dubbo Triple Stub 生成 | 通过 |
| 后端全量回归 | Maven 20 个 Reactor 模块执行测试 | `BUILD SUCCESS`；17 个运行单元健康测试通过 |
| 阶段 2 后端流程 | identity、admin、pet 集成测试 | 各 4 项通过，失败 0 项 |
| 前端静态检查 | web、admin-web 类型检查与 ESLint | 通过 |
| 前端行为测试 | Vitest 3 个测试文件、4 项测试 | 全部通过 |
| 前端生产构建 | web、admin-web Vite build | 全部通过 |
| Compose 静态验证 | 解析 full Compose 配置 | 30 个服务；后端同时接入应用网与数据网 |

## 出口条件

| 条件 | 状态 | 证据 |
|---|---|---|
| 用户与管理会话完全隔离 | 已达成 | 独立 Cookie、独立会话表、独立前端 Store；跨 audience 集成测试通过 |
| RBAC 路由和操作权限有效 | 已达成 | 五个内置角色、细粒度权限和前后端路由守卫已验证 |
| 公共宠物登录后原位切换个人宠物 | 已达成 | 唯一 `GlobalPetHost` 不重建，登录测试验证 `PUBLIC` 原位切换为 `PERSONAL` |

## 未验证项与风险

- Docker 仍未安装，因此尚未执行真实 PostgreSQL、Nacos 与跨进程 Dubbo 的容器联调；当前证据来自 H2 PostgreSQL 兼容模式集成测试、完整构建和 Compose 静态解析。
- 正式 Live2D 模型未提供，宠物身份切换使用静态合法占位渲染验证。
- 高风险管理写操作的密码复验和五分钟一次性作用域令牌将在首个高风险写接口落地时实现；阶段 2 当前仅开放受 RBAC 保护的只读运营概览。

## 用户验收

- 结论：待确认
- 备注：出口条件已有自动化证据，等待用户确认；确认后才更新为“已完成”并进入阶段 3。
