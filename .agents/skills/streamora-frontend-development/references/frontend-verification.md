# 前端验证

1. 执行依赖锁定检查、TypeScript 类型检查、Lint 和单元测试。
2. 对表单、权限、播放器事件、宠物调度器和 SSE 解析执行组件测试。
3. 用户端 E2E 覆盖登录、上传、播放、互动、跨路由宠物和全屏宠物。
4. 管理端 E2E 覆盖角色越权、会话隔离、高风险复验、审计和版本回滚。
5. 验证 360px 至桌面宽度、键盘操作、reduced-motion、断网和资源失败降级。
6. 测量宠物本地规则反应，小于 100ms；语义提示相对播放时间误差小于 500ms。
7. 将真实执行结果写入当前阶段验收文件，未运行的浏览器或性能测试必须明确列出。

